export type User = {
    username: string;
    password: string;
};

export type Report = {
    anythingNew: string;
    comment: string;
    date: string;
    id: number;
    mood: number;
    userId: number;
};